var Logins = {
	ListController: ['$scope', function($scope) {
		$scope.logins = [
			{location: 'http://somelocation.com', user: 'username', password: 'somepass'}
		];
	}],
	CreateController: ['$scope', function($scope) {
		
	}]
}